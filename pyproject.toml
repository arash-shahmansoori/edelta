[project]
name = "nanogpt"
version = "0.0.0"
description = "The simplest, fastest repository for training/finetuning medium-sized GPTs."
dependencies = [
  "torch",
  "numpy",
  "transformers",
  "datasets",
  "tiktoken",
  "wandb",
  "tqdm",
]
